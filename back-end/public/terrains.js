// "_id": 0,
// "name": "Basic",
// "dmg": 5,
// "speed": 10,
// "distance": 600,
// "cd": 500,
// "auto": false,
// "size": 10,
// "hitbox": 50,
// "iconname": "",
// "iconf



class terrain {
    constructor(type, x, y, hitbox) {
        // console.log(type + ":" + x + ":" + y + ":" + intervalX + ":" + intervalY)
        // imageMode(CENTER);
        this.type = type;
        this.x = x;
        this.y = y;
        this.hitbox = hitbox;
    }
}